#pragma once

#include <vector>
#include <string>

namespace NAds::NAlgo::NKmp {

////////////////////////////////////////////////////////////////////////////////

[[nodiscard]] std::vector<std::size_t> kmpSubstrSearch(
    const std::string& text, const std::string& substr);

////////////////////////////////////////////////////////////////////////////////

}  // namespace NAds::NAlgo::NKmp
